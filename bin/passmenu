#!/bin/dash

set -e

IFS='
'
pass=$(cd ~/Passwords && find . -type f -printf '%P\n' | dmenu -p "select password")
echo $pass
pass="$(gpg --pinentry-mode ask -d --ignore-mdc-error -- ~/Passwords/$pass)"
xdotool type "$pass"
