#!/bin/sh

IFS='
'
pass=$(cd ~/Passwords && find . -type f -printf '%P\n' | dmenu)
echo $pass
pass="$(gpg -d -- ~/Passwords/$pass)"
echo -n "$pass" | xclip -selection primary
echo -n "$pass" | xclip -selection clipboard
