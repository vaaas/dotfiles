#!/bin/dash
if ! test -d "$1" -a -f "$2"
then
	echo "bad arguments" > /dev/stderr
	exit 1
fi

src="$2"
dst="$1/$(basename "$src")"
if diff -q -- "$src" "$dst"
then
	echo "$src and $dst are the same"
	rm -v -- "$src"
fi
