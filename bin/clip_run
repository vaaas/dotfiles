#!/usr/bin/env bash

set -e

IFS='
'
stuff='mpv -fs --ytdl-format="best[height<=720]" --
firefox --
emacs --'

clip=$(xclip -selection clipboard -o)
cmd=$(echo "$stuff" | rofi -i -dmenu "Run clipboard on...")
if test -z $cmd
then
	exit 1
fi
eval -- $cmd $clip
